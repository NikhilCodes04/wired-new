const config = {
    API_BASE_URL: import.meta.env.VITE_API_BASE_URL, // Change this URL as needed
    // API_BASE_URL: "http://localhost:8080/api", // Change this URL as needed
};

export default config;